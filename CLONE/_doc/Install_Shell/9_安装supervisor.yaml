
下载安装:
  yum install supervisor -y

自启动:
  systemctl enable supervisord

修改Conf:
  vim /etc/supervisord.conf
  # 拉到最后一行 
  # *.ini 改成 *.conf

复制Conf:
  cp /root/SMSTask/CLONE/Twilio_1/supervisor.conf /etc/supervisord.d/[NAME].conf  
  # [NAME] = 项目名称