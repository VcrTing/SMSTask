更改User为 Root:
  vim /etc/nginx/nginx.conf
  # 更改 user 为 root

移除 Nginx 默认的配置:
  删除 nginx.conf 里面默认的一个 80 端口的配置
  # 注释掉也行

配置Conf:
  cp /root/SMSTask/CLONE/Twilio_1/nginx.http.conf /etc/nginx/conf.d/age.conf
  # cp /root/SMSTask/CLONE/_doc/nginx.https.conf /etc/nginx/conf.d/smstask.conf