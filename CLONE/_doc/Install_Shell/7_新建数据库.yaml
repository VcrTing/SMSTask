
进入:
  mysql -u root -p

新建数据库:
  CREATE DATABASE [NAME] DEFAULT CHARACTER SET utf8 COLLATE utf8_general_ci;
  # [NAME] = 数据库名称。

退出:
  exit;

导入数据给该数据库:
  mysqldump -u root -p [DB] < /root/SMSTask/CLONE/_doc/tsms.sql 
  # [DB] = 目标 数据库名称